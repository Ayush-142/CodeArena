# Reverse a String

You are given a single line string `s` containing only letters and digits (no spaces).
Print the reverse of `s`.

## Input

A single line containing `s` (`1 <= length of s <= 1000`).

## Output

A single line containing the reversed string.
