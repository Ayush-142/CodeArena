# Sum of N Numbers

You are given an integer `n`, followed by `n` integers. Print their sum.

## Input

- Line 1: a single integer `n` (`1 <= n <= 200000`).
- Line 2: `n` space-separated integers, each in `[-1000, 1000]`.

## Output

A single line containing the sum of the `n` integers.
