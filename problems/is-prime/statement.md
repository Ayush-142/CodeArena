# Is It Prime?

You are given a single integer `n`. Print `YES` if `n` is prime, otherwise print `NO`.

A prime number is greater than 1 and has no positive divisors other than 1 and itself.

## Input

A single line containing one integer `n` (`1 <= n <= 1000000`).

## Output

A single line containing `YES` or `NO`.
